should_fire:
  - 'write an interrupt service routine (ISR) for an STM32 microcontroller'
  - 'implement I2C sensor communication in embedded C without blocking'
  - 'configure FreeRTOS task priorities and queue communication for an ESP32'
  - 'optimize RAM and flash memory usage on an ARM Cortex-M device'
  - 'write a circular buffer implementation for UART serial data reception'
  - 'handle low-power sleep modes and wake-up timers in battery IoT firmware'
  - 'debug SPI timing issues with a hardware logic analyzer trace'
  - 'implement a bootloader with firmware update verification checksums'
should_not_fire:
  - prompt: 'build backend API web services in Go or Python'
    expect: 'backend-build'
  - prompt: 'write backend web services in Go or Python'
    expect: 'backend-build'
  - prompt: 'develop mobile native apps in Swift or Kotlin'
    expect: 'mobile-build'
